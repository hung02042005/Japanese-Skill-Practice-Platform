spring:
  application:
    name: jlpt-backend

  datasource:
    url: jdbc:h2:mem:jlptdb_test;MODE=MSSQLServer
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect

  flyway:
    enabled: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: test@example.com
    password: test-password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

jwt:
  secret: test-secret-not-for-production-404E635266556A
  access-expiration-ms: 900000
  refresh-expiration-ms: 604800000

app:
  frontend-url: http://localhost:3000

google:
  client-id: ${GOOGLE_CLIENT_ID:placeholder-replace-with-real-client-id}

logging:
  level:
    "[com.jlpt]": INFO
    "[org.springframework.security]": INFO
    "[org.hibernate.SQL]": WARN
